name: Buildroot Test

on:
  push:
    tags:
      - 'assignment-4-part-1*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        submodules: recursive

    - name: Build Buildroot
      run: |
        cd ${{ github.workspace }}/buildroot
        BR2_EXTERNAL=${{ github.workspace }}/base_external make aesd_defconfig
        BR2_EXTERNAL=${{ github.workspace }}/base_external make

    - name: Run tests
      run: |
        cd ${{ github.workspace }}/assignment-autotest
        ./run-tests.sh

