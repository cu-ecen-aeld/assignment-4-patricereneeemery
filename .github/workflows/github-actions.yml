name: buildroot-test
on:
  push:
    branches:
      - '*'
jobs:
  buildroot-test:
    container: cuaesd/aesd-autotest:24-buildroot
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - name: Run buildroot test
        run: ./full-test.sh
